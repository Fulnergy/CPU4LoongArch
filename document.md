# 信号约定
所有复位信号，低电平有效，即低电平时复位。
所有使能信号，高电平有效。
所有信号宽度，声明时下标[高:低]。
`例：reg [3:0] cnt`

# 数组格式约定
所有数组深度，声明时下标[低:高]
`例：reg [DATA_WIDTH-1:0] mem [0:(1<<ADDR_WIDTH)-1];`

# 数据存储
dMem的最高256byte预留给io
即0xFFFF_F000到0xFFFF_FFFF将映射至输入输出。